Drop the real "One By Nine" images here with these exact filenames:

  exterior.jpg   (night render with NINE branding — main/hero)
  living.jpg     (living room)
  bedroom.jpg    (bedroom)
  kitchen.jpg    (optional)
  bathroom.jpg   (optional)
  amenities.jpg  (optional)

The gallery + map pin use these paths (/images/one-by-nine/<name>.jpg).
If a file is missing, an Unsplash fallback image loads automatically — so
nothing ever shows broken. Drop the files, refresh, and the real images appear.
.jpg, .png, .webp all work (keep the .jpg name in the filename or update the
mock at mock/prototype1/projectDetail.js -> gallery).
